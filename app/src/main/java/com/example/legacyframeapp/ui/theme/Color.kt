package com.example.legacyframeapp.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Café (según Legacy-Frames)
val PrimaryBrown = Color(0xFF8B5C2A)        // --primary-color
val SecondaryBrown = Color(0xFFA6784E)      // --secondary-color
val AccentBrown = Color(0xFFC0894A)         // --accent-color (ajustado para evitar tono anaranjado/rosado)
val DarkBrown = Color(0xFF5C3A1A)           // --dark-color (elevated surfaces)
val LightBackground = Color(0xFFFAF8F5)     // --light-bg
val TextDark = Color(0xFF2C3E50)            // --text-dark
val White = Color(0xFFFFFFFF)               // Blanco
val ErrorRed = Color(0xFFB00020)            // Rojo para errores

// Colores adicionales para modo oscuro profesional
// Café claro (tema claro) y café oscuro (tema oscuro)
val PrimaryCoffeeLight = Color(0xFFB0793E)      // Café claro alternativo (no rosado)
val PrimaryCoffeeDark = Color(0xFF3E2814)       // Café oscuro alternativo
val DarkBackground = Color(0xFF121212)              // Fondo raíz
val DarkSurface = Color(0xFF1E1E1E)                 // Superficie principal
val DarkSurfaceVariant = Color(0xFF262626)          // Variante para listas / chips
val DarkOutline = Color(0xFF3D3D3D)                 // Bordes y divisores sutiles
val PrimaryContainerBrown = Color(0xFF6F451A)       // Contenedor primario
val OnPrimaryContainerBrown = Color(0xFFFFE9D3)     // Texto sobre contenedor primario
val SecondaryContainerBrown = Color(0xFF825F34)     // Contenedor secundario
val OnSecondaryContainerBrown = Color(0xFFFADFC2)   // Texto sobre contenedor secundario
val AccentContainerBrown = Color(0xFFC89257)        // Contenedor terciario/acento
val OnAccentContainerBrown = Color(0xFFFFE9D3)      // Texto sobre contenedor acento
val InverseSurface = Color(0xFFE6E1E5)              // Para estados invertidos (snackbars sobre dark)
val InverseOnSurface = Color(0xFF1E1E1E)            // Texto sobre inverseSurface
