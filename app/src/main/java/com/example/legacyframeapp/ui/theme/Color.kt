package com.example.legacyframeapp.ui.theme

import androidx.compose.ui.graphics.Color

// Colores de tu marca "Legacy Frames"
val PrimaryBrown = Color(0xFF8B5C2A) // Café principal (para TopBar, botones)
val SecondaryBrown = Color(0xFFA6784E) // Café secundario
val AccentBrown = Color(0xFFD4A574) // Café claro (para acentos)
val DarkBrown = Color(0xFF5C3A1A) // Café oscuro (para bordes, hover)
val LightBackground = Color(0xFFFAF8F5) // Fondo claro de la app
val TextDark = Color(0xFF2c3e50) // Texto oscuro principal
val White = Color(0xFFFFFFFF) // Blanco
val ErrorRed = Color(0xFFB00020) // Rojo para errores